apiVersion: v1
kind: Service
metadata:
  name: {{ include "common.names.fullname" . }}-slurmd
  namespace: {{ include "common.names.namespace" . | quote }}
  labels: {{- include "common.labels.standard" ( dict "customLabels" .Values.slurmd.commonLabels "context" $ ) | nindent 4 }}
  annotations:
    {{- $annotations := include "common.tplvalues.merge" ( dict "values" ( list .Values.slurmd.service.annotations .Values.slurmd.commonAnnotations ) "context" . ) }}
    {{- include "common.tplvalues.render" ( dict "value" $annotations "context" $) | nindent 4 }}
spec:
  type: ClusterIP
  ports:
    {{- if .Values.slurmd.service.port }}
    - name: slurmd
      port: {{ .Values.slurmd.service.port }}
      targetPort: {{ .Values.slurmd.service.targetPort }}
      {{- if and (or (eq .Values.slurmd.service.type "NodePort") (eq .Values.slurmd.service.type "LoadBalancer")) (not (empty .Values.slurmd.service.nodePort)) }}
      nodePort: {{ .Values.slurmd.service.nodePort }}
      {{- end }}
    {{- end }}
    {{- if .Values.slurmd.service.extraPorts }}
    {{- include "common.tplvalues.render" (dict "value" .Values.slurmd.service.extraPorts "context" $) | nindent 4 }}
    {{- end }}
  {{- $podLabels := include "common.tplvalues.merge" ( dict "values" ( list .Values.slurmd.podLabels .Values.slurmd.commonLabels ) "context" . ) }}
  selector: {{- include "common.labels.matchLabels" ( dict "customLabels" $podLabels "context" $ ) | nindent 4 }}
    app.kubernetes.io/component: slurmd
